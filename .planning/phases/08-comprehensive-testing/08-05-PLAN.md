---
phase: 08-comprehensive-testing
plan: 05
type: execute
domain: testing
---

<objective>
End-to-end tests for critical user flows using Playwright.

Purpose: Verify complete user journeys work correctly in the browser.
Output: E2E test coverage for landing page, demo flow, and dashboard navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@e2e/demo.test.ts (existing E2E test)
@playwright.config.ts
@src/routes/+page.svelte (landing page)
@src/routes/try/+page.svelte (demo page)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing Page E2E Tests</name>
  <files>e2e/landing.test.ts</files>
  <action>
Create E2E tests for landing page:

**Page load tests:**
- Landing page loads successfully
- Has visible h1 heading
- Navigation links are present

**Hero section tests:**
- "Try It Free" button is visible and clickable
- "See Pricing" button scrolls to pricing section

**Pricing section tests:**
- All 4 pricing tiers displayed (Free Demo, Single Repo, Pro, Team)
- Pricing amounts are correct ($39, $99/mo, $249/mo)
- CTA buttons present for each tier

**FAQ section tests:**
- FAQ accordion items are present
- Clicking expands/collapses FAQ items

**Navigation tests:**
- Header contains logo
- Login/signup links visible
- Mobile menu toggle works on small screens

Use Playwright's test() and expect() with page locators.
  </action>
  <verify>npm run test:e2e -- e2e/landing.test.ts passes</verify>
  <done>10+ E2E tests for landing page</done>
</task>

<task type="auto">
  <name>Task 2: Demo Page E2E Tests</name>
  <files>e2e/try.test.ts</files>
  <action>
Create E2E tests for demo/try page:

**Page load tests:**
- Demo page loads at /try route
- Has form for repo URL input
- Submit button is present

**Form interaction tests:**
- Can type in repo URL input
- Invalid URL shows error state
- Valid URL enables submit

**UI elements tests:**
- Shows usage limit info
- Has link to sign up for more
- Displays locked doc preview after generation

Note: Don't test actual generation (would hit API). Focus on UI interactions.
  </action>
  <verify>npm run test:e2e -- e2e/try.test.ts passes</verify>
  <done>8+ E2E tests for demo page</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>E2E test suite for landing and demo pages</what-built>
  <how-to-verify>
    1. Run: npm run test:e2e
    2. Verify all tests pass
    3. Check test output shows reasonable coverage
    4. Confirm no flaky tests (run twice)
  </how-to-verify>
  <resume-signal>Type "approved" if tests pass consistently, or describe any flaky tests</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All E2E tests pass locally
- [ ] Tests run in reasonable time (<30s)
- [ ] No flaky tests
- [ ] Tests cover critical user paths
</verification>

<success_criteria>
- All tasks completed
- 18+ E2E tests passing
- Landing page fully tested
- Demo page interactions tested
- Tests are stable and repeatable
</success_criteria>

<output>
After completion, create `.planning/phases/08-comprehensive-testing/08-05-SUMMARY.md`
</output>
